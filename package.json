{
  "name": "opencode-sync-memory",
  "version": "0.3.0",
  "description": "Persistent memory for opencode, synced across machines via a private GitHub repo",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": ["dist", "README.md", "LICENSE"],
  "keywords": ["opencode", "plugin", "memory", "sync", "encryption", "ai"],
  "license": "MIT",
  "dependencies": {
    "@opencode-ai/plugin": "^1.15.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  },
  "scripts": {
    "build": "tsc",
    "test": "node --test src/__tests__/*.test.ts",
    "test:watch": "node --test --watch src/__tests__/*.test.ts",
    "lint": "tsc --noEmit",
    "prepublishOnly": "npm run build",
    "pretest": "npm run build"
  }
}
